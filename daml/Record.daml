module Record where

import Daml.Finance.Interface.Types.Common.V3.Types (Id)

template Record
	with
		id : Id
		patient : Party
		department : Text --Department
	where
    signatory patient
    observer patient --department
    key (id, patient, department) : (Id, Party, Text)
    maintainer key._2	